package;

import civo.Charges;
import civo.Dns;
import civo.Firewalls;
import civo.Instances;
import civo.Kubernetes;
import civo.LoadBalancers;
import civo.Networks;
import civo.Quota;
import civo.Regions;
import civo.Snapshots;
import civo.Ssh;
import civo.Templates;
import civo.Volumes;
import civo.Webhooks;

class Civo {
  static function main() {}
}