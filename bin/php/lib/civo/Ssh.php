<?php
/**
 * Generated by Haxe 4.0.2
 */

namespace civo;

use \php\_Boot\HxAnon;
use \php\Boot;
use \civo\net\CivoHttp;

class Ssh {
	/**
	 * @var string
	 */
	static public $path = "/sshkeys";

	/**
	 * @param string $token
	 * @param string $ssh_id
	 * @param \Closure $handler
	 * 
	 * @return void
	 */
	public static function delete ($token, $ssh_id, $handler) {
		#src/civo/Ssh.hx:33: characters 5-53
		CivoHttp::delete($token, "" . (Ssh::$path??'null') . "/" . ($ssh_id??'null'), $handler);
	}

	/**
	 * @param string $token
	 * @param string $ssh_id
	 * @param \Closure $handler
	 * 
	 * @return void
	 */
	public static function get ($token, $ssh_id, $handler) {
		#src/civo/Ssh.hx:21: characters 5-50
		CivoHttp::get($token, "" . (Ssh::$path??'null') . "/" . ($ssh_id??'null'), $handler);
	}

	/**
	 * @param string $token
	 * @param \Closure $handler
	 * 
	 * @return void
	 */
	public static function list ($token, $handler) {
		#src/civo/Ssh.hx:17: characters 5-39
		CivoHttp::get($token, Ssh::$path, $handler);
	}

	/**
	 * @param string $token
	 * @param string $ssh_id
	 * @param string $name
	 * @param \Closure $handler
	 * 
	 * @return void
	 */
	public static function update ($token, $ssh_id, $name, $handler) {
		#src/civo/Ssh.hx:29: characters 5-64
		CivoHttp::put($token, "" . (Ssh::$path??'null') . "/" . ($ssh_id??'null'), $handler, new HxAnon(["name" => $name]));
	}

	/**
	 * @param string $token
	 * @param string $name
	 * @param string $public_key
	 * @param \Closure $handler
	 * 
	 * @return void
	 */
	public static function upload ($token, $name, $public_key, $handler) {
		#src/civo/Ssh.hx:25: characters 5-78
		CivoHttp::post($token, Ssh::$path, $handler, new HxAnon([
			"name" => $name,
			"public_key" => $public_key,
		]));
	}
}

Boot::registerClass(Ssh::class, 'civo.Ssh');
